[pytest]
testpaths = tests
addopts = -ra
markers =
    l2: small-model mechanics (loads SmolLM2 + MiniLM; enable with RUN_L2=1)
    l3: GPU closed-loop judge tests (enable with RUN_L3=1; NEVER while tuner/night_run is active)
