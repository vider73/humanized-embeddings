# Motor de steering — dependencias
torch>=2.1
transformers>=4.44
accelerate>=0.33
sentence-transformers>=2.7
numpy
tqdm

# Solo si usas LOAD_4BIT=True (Linux/GPU). En Windows puede dar guerra:
# si falla, pon LOAD_4BIT=False en config.py (fp16 cabe en tu 24GB).
bitsandbytes>=0.43 ; platform_system != "Windows"
